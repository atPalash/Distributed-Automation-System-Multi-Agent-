*Run orders_Agent.js. Starts server at localhost:4500.
*Make order at the HTML file which opens with GET call at localhost:4500.
*Run app.js. Starts server for Workstations and loads a Pallet with its own dedicated server.
*After 10secs another pallet is inserted automaticaly to the system.
*Error of undefined PalletID or receiving multiple events may occur, just reset the SImulator and start again. 